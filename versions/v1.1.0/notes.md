# Notes for v1.1.0

- Extended from v1.0.0 to add Stopwatch with basic controls.
- Used `datetime` for time calculations in Stopwatch.
- Switch button uses `pack_forget()` and `pack()` for mode transition.
- Next steps: Refine Stopwatch logic and add mark feature.